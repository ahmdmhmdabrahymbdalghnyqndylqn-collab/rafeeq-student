# Supabase backend

The database schema, RLS policies, storage bucket, RPCs, and membership triggers are already deployed to project `ivjqorxkqnipyelnjlqp`.

`functions/rafiq-admin/index.ts` is the source snapshot of the deployed administrator Edge Function. Do not deploy this directory as frontend assets or blindly reapply historical migrations to the existing project.
